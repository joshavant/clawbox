## Summary

Describe what changed and why.

## Validation

- [ ] Ran `./scripts/ci/run.sh fast`
- [ ] Ran `./scripts/ci/run.sh logic`
- [ ] Ran `./scripts/ci/run.sh integration` (if macOS + Tart environment was available)

## Maintainer Notes

- [ ] If this PR comes from a fork and requires VM integration, add the `safe-to-test` label to allow the `VM Integration` workflow to run.
